Recovered archive created after the previous directory-style sandbox link failed.

Important: only the following modified files from the prior work were present in the persisted workspace:
- src/app/background_tasks.rs
- src/app/background_task_runtime.rs

The remaining files are from the originally uploaded AuRaw-2.0 source archive. Therefore this recovered archive should not be treated as a verified complete implementation of all modifications claimed in the prior response.
